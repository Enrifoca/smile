/** Dedicated macOS title bar strip for native traffic lights (GitHub Desktop–style). */
export default function MacWindowChrome() {
  return <div className="mac-titlebar drag-region shrink-0" aria-hidden="true" />
}
