{
  "apiVersion": "1.0",
  "id": "reference-fixture",
  "name": "Contract reference fixture",
  "version": "0.0.0",
  "description": "Minimal manifest used by host contract checks — not a shippable connector.",
  "auth": {
    "type": "api-key",
    "fields": [{ "key": "apiToken", "label": "API token", "secret": true }]
  },
  "permissions": {
    "http": ["https://api.example.com"],
    "mcp": [],
    "file": { "read": true, "write": false },
    "secrets": ["apiToken"]
  },
  "ui": { "catalogLabel": "Reference", "scopeLabel": "Workspaces" },
  "tools": [
    {
      "name": "reference_search_records",
      "description": "Search records by query",
      "category": "connector-read",
      "requiresConfirmation": false,
      "inputSchema": {
        "type": "object",
        "properties": { "query": { "type": "string" } },
        "required": ["query"]
      }
    },
    {
      "name": "reference_create_record",
      "description": "Create a record",
      "category": "connector-write",
      "requiresConfirmation": true,
      "inputSchema": {
        "type": "object",
        "properties": { "title": { "type": "string" }, "body": { "type": "string" } },
        "required": ["title"]
      },
      "confirmation": { "title": "Create record", "summary": "Create \"{{title}}\"" },
      "preview": "Create record: {{title}}"
    }
  ]
}
