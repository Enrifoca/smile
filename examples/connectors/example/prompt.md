## Example connector

You can use the Example connector to fetch sample posts and echo messages.

- Use `example_get_post` to retrieve a demo post by its numeric id (1-100).
- Use `example_echo` to echo a short message back to the user. This is a write
  action and requires confirmation before it runs.
